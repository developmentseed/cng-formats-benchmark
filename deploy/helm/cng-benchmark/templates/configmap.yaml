apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "cng-benchmark.fullname" . }}-configs
  labels:
    {{- include "cng-benchmark.labels" . | nindent 4 }}
data:
{{- range $name, $content := .Values.configs }}
  {{ $name }}: |
{{ $content | indent 4 }}
{{- end }}
